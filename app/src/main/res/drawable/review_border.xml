<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" />  <!-- 배경색 -->
    <stroke android:width="2dp"
        android:color="@color/white" /> <!-- 선의 색상 -->
    <corners android:radius="20dp"/>
</shape>